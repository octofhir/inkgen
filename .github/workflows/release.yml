name: Release

on:
  workflow_dispatch:
    inputs:
      version_bump:
        description: 'Version bump type'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
          - custom
        default: 'patch'
      custom_version:
        description: 'Custom version (only used if version_bump is "custom", e.g., 1.2.3)'
        required: false
        type: string
      dry_run:
        description: 'Dry run (no commits or tags)'
        required: false
        type: boolean
        default: false
      create_release:
        description: 'Create GitHub release after version bump'
        required: false
        type: boolean
        default: true

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    outputs:
      new_version: ${{ steps.new_version.outputs.new_version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-edit
        run: cargo install cargo-edit --locked

      - name: Get current version
        id: current_version
        run: |
          CURRENT_VERSION=$(grep -m1 '^version = ' Cargo.toml | sed 's/version = "\(.*\)"/\1/')
          echo "version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $CURRENT_VERSION"

      - name: Calculate new version
        id: new_version
        run: |
          CURRENT="${{ steps.current_version.outputs.version }}"
          BUMP="${{ inputs.version_bump }}"
          CUSTOM="${{ inputs.custom_version }}"

          if [ "$BUMP" = "custom" ]; then
            if [ -z "$CUSTOM" ]; then
              echo "Error: Custom version is required when version_bump is 'custom'"
              exit 1
            fi
            NEW_VERSION="$CUSTOM"
          else
            IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"
            case "$BUMP" in
              major)
                NEW_VERSION="$((MAJOR + 1)).0.0"
                ;;
              minor)
                NEW_VERSION="${MAJOR}.$((MINOR + 1)).0"
                ;;
              patch)
                NEW_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))"
                ;;
            esac
          fi

          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "New version: $NEW_VERSION"

      - name: Update workspace version
        if: ${{ !inputs.dry_run }}
        run: |
          NEW_VERSION="${{ steps.new_version.outputs.new_version }}"

          # Update workspace version in root Cargo.toml
          sed -i "s/^version = \".*\"/version = \"$NEW_VERSION\"/" Cargo.toml

          echo "Updated Cargo.toml to version $NEW_VERSION"
          grep -A5 '\[workspace.package\]' Cargo.toml

      - name: Verify changes
        run: |
          cargo check --workspace
          cargo test --workspace --lib

      - name: Commit version bump
        if: ${{ !inputs.dry_run }}
        run: |
          NEW_VERSION="${{ steps.new_version.outputs.new_version }}"
          git add Cargo.toml
          git commit -m "chore: bump version to $NEW_VERSION"
          echo "Committed version bump"

      - name: Create and push tag
        if: ${{ !inputs.dry_run }}
        run: |
          NEW_VERSION="${{ steps.new_version.outputs.new_version }}"
          git tag -a "v$NEW_VERSION" -m "Release v$NEW_VERSION"
          git push origin main --follow-tags
          echo "Pushed tag v$NEW_VERSION"

      - name: Generate changelog
        if: ${{ !inputs.dry_run && inputs.create_release }}
        id: changelog
        run: |
          cargo install git-cliff --locked
          git-cliff --latest --strip header > /tmp/CHANGELOG.md
          echo "Generated changelog:"
          cat /tmp/CHANGELOG.md

      - name: Create GitHub Release
        if: ${{ !inputs.dry_run && inputs.create_release }}
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.new_version.outputs.new_version }}
          name: Release v${{ steps.new_version.outputs.new_version }}
          body_path: /tmp/CHANGELOG.md
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Dry run summary
        if: ${{ inputs.dry_run }}
        run: |
          echo "=== DRY RUN SUMMARY ==="
          echo "Current version: ${{ steps.current_version.outputs.version }}"
          echo "New version would be: ${{ steps.new_version.outputs.new_version }}"
          echo "Version bump type: ${{ inputs.version_bump }}"
          echo ""
          echo "Changes that would be made:"
          echo "  - Update version in Cargo.toml"
          echo "  - Commit: 'chore: bump version to ${{ steps.new_version.outputs.new_version }}'"
          echo "  - Create tag: v${{ steps.new_version.outputs.new_version }}"
          if [ "${{ inputs.create_release }}" = "true" ]; then
            echo "  - Create GitHub release"
          fi

  build-binaries:
    name: Build Release Binaries
    needs: release
    if: ${{ !inputs.dry_run }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: inkgen-cli
            asset_name: inkgen-cli-linux-amd64
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            artifact_name: inkgen-cli
            asset_name: inkgen-cli-linux-arm64
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: inkgen-cli
            asset_name: inkgen-cli-darwin-amd64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: inkgen-cli
            asset_name: inkgen-cli-darwin-arm64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: inkgen-cli.exe
            asset_name: inkgen-cli-windows-amd64.exe

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools (Linux ARM64)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release -p inkgen-cli --target ${{ matrix.target }}
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Upload release asset
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ needs.release.outputs.new_version }}
          files: target/${{ matrix.target }}/release/${{ matrix.artifact_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
